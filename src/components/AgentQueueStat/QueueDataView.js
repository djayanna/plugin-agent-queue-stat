import {
    Table,
    TableBody,
    TableRow,
    TableCell,
  } from "@material-ui/core";

class QueueDataView extends React.Component {

    constructor(props) {
        super(props);
      }

  render() {
      const {queueList} = this.props;
    return (
      <div>
        
      </div>
    );
  }
}

export default QueueDataView;